name: Keep Bot Alive
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: |
          curl -s https://kozgu-app.onrender.com
          echo "Pinged at $(date)"
